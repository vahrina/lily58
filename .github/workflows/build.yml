on:
  push:
    paths:
    - 'config/**/*.conf'
    - 'config/**/*.keymap'
    - 'build.yaml'
    - 'config/west.yml'
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
